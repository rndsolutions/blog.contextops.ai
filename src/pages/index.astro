---
const blogUrl = '/blog';

// If running on the server, perform a server-side redirect:
if (Astro.request) {
  return Astro.redirect(blogUrl, 301);
}
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Fallback for static sites: a meta refresh redirect -->
    <meta http-equiv="refresh" content="0; url={blogUrl}" />
    <title>Redirecting to Blog…</title>
  </head>
  <body>
    <p>
      If you are not redirected automatically, follow this
      <a href={blogUrl}>link to the blog</a>.
    </p>
  </body>
</html>
